"""Test suite for ehs-human-factors-ontology.

Run with::

    python3 -m unittest discover -s tests -t .

``tests/context.py`` puts ``src/`` on the path so the package can be imported
without being installed.
"""
